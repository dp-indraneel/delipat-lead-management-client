import PendingApiPage from "./PendingApiPage";

export default function SettingsPage() {
  return (
    <PendingApiPage
      title="Settings"
      description="This screen stays reusable, but the backend settings API has not been shared yet."
    />
  );
}
